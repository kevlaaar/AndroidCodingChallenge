<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageView
        android:id="@+id/teamBubbleColor"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginStart="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>
    <TextView
        android:id="@+id/teamText"
        android:layout_width="0dp"
        android:layout_height="20dp"
        android:text="PROGRAMER"
        android:lines="1"
        android:gravity="center_vertical"
        android:textSize="12sp"
        android:textColor="#000"
        android:layout_marginStart="10dp"
        app:layout_constraintTop_toTopOf="@id/teamBubbleColor"
        app:layout_constraintBottom_toBottomOf="@id/teamBubbleColor"
        app:layout_constraintStart_toEndOf="@id/teamBubbleColor"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>